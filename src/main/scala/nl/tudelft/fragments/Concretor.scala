package nl.tudelft.fragments

object Concretor {
  // Concretize the rule's pattern by assigning concrete names and types
  def concretize(rule: Rule, diseq: List[Diseq]): Pattern = {
    // val names = rule.pattern.names

    // TODO: Here you can choose for as few distinct names (satisfying the disequality constraints) or as many as possible or anywhere in between

    ???
  }
}
