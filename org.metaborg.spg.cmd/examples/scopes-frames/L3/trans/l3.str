module l3

imports
  
  completion/completion
  pp
  outline
  analysis

// This is only needed to satisfy TypeSmart and the strj compiler when compiling the language
signature
  constructors
    Conss : a * List(a) -> Iter

rules // Debugging
  
  debug-show-aterm:
    (node, _, _, path, project-path) -> (filename, result)
    with
      filename := <guarantee-extension(|"aterm")> path
    ; result   := node
